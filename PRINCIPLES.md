# RL_SuperMario 项目原理与参数详解

本文档面向刚接触强化学习的读者，从零讲解这个项目的每一部分。

---

## 目录

1. [这个项目在做什么](#1-这个项目在做什么)
2. [强化学习核心概念](#2-强化学习核心概念)
3. [PPO 算法原理](#3-ppo-算法原理)
4. [环境与预处理](#4-环境与预处理)
5. [神经网络结构](#5-神经网络结构)
6. [训练管线](#6-训练管线)
7. [超参数详解](#7-超参数详解)
8. [调参指南](#8-调参指南)

---

## 1. 这个项目在做什么

用 PPO 算法训练一个 AI，让它学会玩**超级马里奥兄弟（NES 版）第一关**。AI 只能看到游戏画面（84×84 灰度图），通过学习"什么画面下按什么键能得到高分"，最终学会通关。

```
游戏画面(84×84 灰度) ──→ 神经网络 ──→ 7 个动作之一
                                      ├── 不动
                                      ├── 右
                                      ├── 右 + 跳
                                      ├── 右 + 跑
                                      ├── 右 + 跑 + 跳
                                      ├── 左
                                      └── 左 + 跳
```

---

## 2. 强化学习核心概念

### 2.1 基本框架

```
         ┌──────────────────────────┐
         │      Environment          │
         │      (NES 模拟器)         │
         │                          │
         │  state (游戏画面)          │
         │      ↓                   │
         │  agent 选 action          │
         │      ↓                   │
         │  reward (得分变化)        │
         │      ↓                   │
         │  新 state (下一帧画面)     │
         └──────────────────────────┘
```

- **Agent（智能体）**：我们的 PPO 模型，看画面 → 决定按键
- **Environment（环境）**：NES 模拟器上的超级马里奥
- **State（状态）**：当前游戏画面（4 帧堆叠的 84×84 灰度图）
- **Action（动作）**：7 种按键组合
- **Reward（奖励）**：马里奥向右移动的距离 + 额外奖励（金币、通关等）
- **Episode（回合）**：从开始到死亡/通关的一局游戏

### 2.2 强化学习和监督学习的区别

| | 监督学习 | 强化学习 |
|---|---|---|
| 数据 | 人标注的（猫/狗） | AI 自己玩出来的 |
| 反馈 | 每张图都有正确答案 | 只有延迟的"做得好不好" |
| 目标 | 分类准确 | 累计奖励最大 |
| 难点 | 标注成本 | 探索 vs 利用的平衡 |

### 2.3 关键术语

**Policy（策略）π(a|s)**：给定状态 s，选择动作 a 的概率分布。"看见这个画面，70% 概率跳，30% 概率不动"

**Value（价值）V(s)**：从状态 s 开始，按照当前策略能拿到的期望总奖励。"站在这个位置，大概还能拿 500 分"

**Advantage（优势）A(s,a)**：在状态 s 做动作 a，比平均水平好多少。"这里跳比不跳多拿 20 分"

**Return（回报）**：一个 episode 里的累计奖励，带折扣因子 γ：

```
Return = r₀ + γ·r₁ + γ²·r₂ + γ³·r₃ + ...
```

γ 越接近 1，越看重远期回报；越接近 0，越短视。

---

## 3. PPO 算法原理

### 3.1 为什么选 PPO

PPO（Proximal Policy Optimization）是 Stable-Baselines3 的默认算法，因为：

| 特性 | 说明 |
|------|------|
| 稳定 | 不会突然崩掉（相比 TRPO、A2C） |
| 简单 | 超参数少，大部分默认就够用 |
| 快 | on-policy 但采样效率合理 |
| 适合 | 离散动作、连续动作都行，图像输入也支持 |

### 3.2 PPO 的核心想法

**旧策略**收集一批数据 → **新策略**在这些数据上学，但**不能偏离旧策略太远**。

```
     旧策略                    新策略
  ┌─────────┐             ┌─────────┐
  │  π_old  │── 采集数据 →│  π_new  │
  └─────────┘             └─────────┘
                                │
                   每次更新不能偏离 π_old 太多
                   用 clip_range 限制了更新幅度
```

### 3.3 Clip 机制

PPO 的损失函数里有一个 clip 操作，防止一次更新太大：

```python
ratio = π_new(a|s) / π_old(a|s)  # 新策略和旧策略的概率比
# 当 advantage > 0（好动作）：ratio 不能超过 1 + clip_range
# 当 advantage < 0（坏动作）：ratio 不能低于 1 - clip_range
```

**打个比方**：你觉得这道题选 A（概率比 1.0），学完之后你 100% 选 A（概率比变成 10.0）。clip 告诉你"最多涨到 1.1 就行，别太自信，可能就是蒙对的"。

### 3.4 GAE（广义优势估计）

用来计算 Advantage。参数 `gae_lambda` 控制 bias-variance 平衡：

```
λ = 0：只看眼前一步，方差小但偏差大
λ = 1：看整条轨迹，偏差小但方差大
λ = 0.95：折中（最常用的值）
```

### 3.5 KL 早停

PPO 每轮最多更新 `n_epochs` 次。但如果新旧策略的 KL 散度超过 `target_kl`，就提前停止——说明这轮数据已经学够了。

---

## 4. 环境与预处理

### 4.1 原始环境

```python
env = gym_super_mario_bros.make('SuperMarioBros-v2')
```

- 游戏：超级马里奥兄弟（NES，世界 1-1）
- 原始画面：**240×256 像素，RGB 彩色**
- 原始动作：**256 种**（NES 手柄所有按键组合）
- 帧率：**60 fps**

### 4.2 环境包装器链

每一步数据经过 6 层包装器处理：

```
原始 NES 环境
  ↓ JoypadSpace           → 256 种动作 → 7 种简化动作
  ↓ RewardWrapper         → 叠加自定义奖励
  ↓ SkipFrame(4)           → 每 4 帧跳 3 帧，累积奖励
  ↓ GrayScaleObservation   → RGB → 灰度
  ↓ ResizeObservation(84)  → 240×256 → 84×84
  ↓ Monitor               → 记录 episode 奖励到 CSV
  ↓ VecFrameStack(4)       → 堆叠最近 4 帧（含运动信息）
```

#### 4.2.1 JoypadSpace

把 256 种 NES 按键组合 → 7 种常用动作：

| 编号 | 动作 | 说明 |
|------|------|------|
| 0 | NOOP | 什么都不按 |
| 1 | Right | 向右走 |
| 2 | Right + A | 向右 + 跳 |
| 3 | Right + B | 向右 + 加速跑 |
| 4 | Right + A + B | 向右 + 跑 + 跳（远跳） |
| 5 | A | 原地跳 |
| 6 | Left | 向左走 |

#### 4.2.2 SkipFrame(4)

每 4 帧只处理 1 帧。连续帧画面几乎不变，跳帧不会丢信息，但把有效帧率从 60fps 降到 15fps，减少 75% 的计算量。

```
原始帧: F0 F1 F2 F3 F4 F5 F6 F7 F8 F9 F10 F11 ...
处理后: F0          F4          F8           ...
动作:   同一个动作执行 4 次          同一个动作执行 4 次
奖励:   4 帧奖励累加                4 帧奖励累加
```

#### 4.2.3 GrayScale + Resize

- 灰度：马甲颜色不重要，通关才是关键。去掉颜色减少输入维度
- 84×84：经典 Atari RL 的标准尺寸，足够分辨游戏元素（问号方块、水管、敌人）。

#### 4.2.4 VecFrameStack(4)

把最近 4 帧堆叠成一个"方块"：

```
输入维度: (4, 84, 84)
  ├── 第 1 层：t-3 时刻的灰度图
  ├── 第 2 层：t-2 时刻的灰度图
  ├── 第 3 层：t-1 时刻的灰度图
  └── 第 4 层：t   时刻的灰度图
```

**为什么要堆 4 帧？** 单帧看不出运动方向。马里奥在动还是敌人在动，必须对比前后帧才知道。

### 4.3 奖励设计

#### 基础奖励（gym 自带）

每步的奖励 = **x 轴位移**（马里奥向右移动了多少像素）。这是最主要的奖励信号。

#### 自定义奖励（RewardWrapper）

| 事件 | 奖励值 | 目的 |
|------|--------|------|
| 通关（碰到旗杆） | +500 | 通关是第一目标 |
| 吃金币 | +10/枚 | 适度鼓励，不喧宾夺主 |
| 死亡 | -50/次 | 有痛感但不崩盘 |
| 卡住不动（100+ 步） | -1/步 | 推动探索 |

**设计原则**：额外奖励要和基础奖励（+1~15/步）同量级，不能让 AI 为吃金币放弃通关。

---

## 5. 神经网络结构

PPO 默认使用 **CnnPolicy**，在 84×84 的 4 帧输入上跑 CNN：

```
输入: (4, 84, 84)
  ↓
Conv2d(4→32, kernel=8, stride=4) → ReLU    # → (32, 20, 20)
  ↓
Conv2d(32→64, kernel=4, stride=2) → ReLU   # → (64, 9, 9)
  ↓
Conv2d(64→64, kernel=3, stride=1) → ReLU   # → (64, 7, 7)
  ↓
Flatten → (3136,)
  ↓
Linear(3136→512) → ReLU
  ↓
        ┌──────────────┬──────────────┐
        ↓              ↓              ↓
   Actor 头        Critic 头
   Linear→7       Linear→1
   (动作概率)      (价值估计)
```

- **Actor（策略网络）**：输出 7 个动作的概率分布。训练时按概率采样（带探索），测试时选概率最大的
- **Critic（价值网络）**：输出一个数值 V(s)，估计当前局面的好坏。用来计算 Advantage

---

## 6. 训练管线

### 6.1 并行环境

```
SubprocVecEnv(14 个进程)

进程1: env ─┐
进程2: env ─┤
  ...       ├──→ 14 条轨迹同时收集
进程14: env ─┘

每步收集 14 个 (obs, action, reward)
训练时打乱成 batch = 8192
```

14 个环境并行跑，数据收集速度快 14 倍。

### 6.2 一个训练周期的完整流程

```
第 1 步：14 个环境各收集 1024 步
        └── 总共 14×1024 = 14336 条经验

第 2 步：用 GAE 计算每条经验的 Advantage

第 3 步：10 个 epoch 的数据复用
        ├── 每条数据被 shuffle
        ├── 每次取 8192 条做一次梯度更新
        └── 如果 KL 散度超过 0.03 → 提前停

第 4 步：回到第 1 步，用更新后的策略继续收集
```

### 6.3 模型保存机制

每 10 万步检查一次最近 100 个 episode 的平均奖励。比之前最好的高 → 保存模型。

---

## 7. 超参数详解

### 7.1 训练规模

| 参数 | 默认值 | 作用 |
|------|--------|------|
| `total_timesteps` | 40,000,000 | 总共训练多少步。Mario 这个级别约需 20-40M 步 |
| `num_envs` | 14 | 并行环境数。等于可用 CPU 核心数-2 |
| `check_freq` | 100,000 | 多少步检查一次模型是否该保存 |

### 7.2 PPO 核心参数

| 参数 | 默认值 | 作用 | 调大 | 调小 |
|------|--------|------|------|------|
| `learning_rate` | 3e-4 | 每次更新的步长 | 学得快但可能崩 | 学得慢但稳定 |
| `n_steps` | 1024 | 每个环境收集多少步再做一次更新 | 数据多样但更新慢 | 更新频繁但数据少 |
| `batch_size` | 8192 | 每次梯度更新用多少条数据 | 梯度稳定，GPU 利用率高 | 节省显存 |
| `n_epochs` | 10 | 同一批数据反复学几次 | 数据利用率高 | 防止过拟合 |
| `gamma` | 0.95 | 折扣因子，看多远 | 看重远期（通关） | 看重当下 |
| `gae_lambda` | 0.95 | GAE 平滑系数 | 看重全轨迹 | 看重眼前 |
| `clip_range` | 0.1 | 策略更新的最大变化幅度 | 允许大变化 | 更保守 |
| `target_kl` | 0.03 | KL 早停阈值 | 允许学到更多 | 更早停，更保守 |
| `ent_coef` | 0.1 | 熵系数，鼓励探索 | 更多探索（乱按） | 更确定（重复） |
| `vf_coef` | 0.5 | 价值函数损失权重 | 更看重价值估计准确 | 更看重策略优化 |
| `max_grad_norm` | 0.8 | 梯度裁剪上限 | 允许大梯度 | 防止梯度爆炸 |

### 7.3 参数之间的关联

```
n_steps × num_envs ≈ batch_size 的整数倍

当前: 1024 × 14 = 14336 ≈ 8192 × 1.75
      （1.75 个 mini-batch，不够整数倍但 SB3 会自动处理）

理想: 1024 × 16 = 16384 = 8192 × 2
      （刚好 2 个 mini-batch，GPU 利用率最高）
```

---

## 8. 调参指南

### 8.1 遇到这些问题怎么调

| 现象 | 可能原因 | 调整方向 |
|------|---------|---------|
| reward 不涨 | 探索不够 | 增大 `ent_coef`（0.1→0.2） |
| reward 剧烈震荡 | 学太快 | 减小 `learning_rate`（3e-4→1e-4），减小 `clip_range` |
| loss=NaN | 梯度爆炸 | 减小 `learning_rate`，减小 `max_grad_norm` |
| KL 每轮都在第 2-3 epoch 停 | 更新太激进 | 减小 `learning_rate`，增大 `target_kl` |
| KL 从不停（跑满 10 epoch） | 学太慢 | 增大 `learning_rate` |
| 只往右冲，不吃金币 | 探索 vs 利用偏了 | 增大 `ent_coef`，或增加金币奖励 |
| GPU 利用率很低 | CNN 太小，CPU 瓶颈 | 增大 `num_envs`，减小 `n_steps` |
| 显存不够 | batch 太大 | 减小 `batch_size`，减少 `num_envs` |

### 8.2 什么时候停止训练

看 TensorBoard 的 `rollout/ep_rew_mean` 曲线：

```
上升期 → 继续训
平台期 → 再跑 5M 步，还是不涨就停
下降期 → 可能过拟合了，取平台期的模型
```

这个项目的经验值：20-40M 步之间收敛。

### 8.3 建议的调参顺序

1. **先跑默认参数**，确保环境正常、reward 在涨
2. **调 `num_envs`**，匹配 CPU 核心数，最大化 FPS
3. **调 `learning_rate`**，用 1e-4、3e-4、5e-4 各跑 2M 步，选 reward 涨最快的
4. **调 `ent_coef`**，如果 AI 行为太单一（比如永远右跳），就加大
5. **最后调奖励**，每次只改一个奖励项，对比效果

---

## 附录：文件速查

| 文件 | 作用 |
|------|------|
| `train.py` | 训练入口：搭环境、设参数、执行训练 |
| `util_class.py` | 自定义包装器：帧跳过、奖励、模型保存回调 |
| `test_model.py` | 加载训练好的模型，渲染游戏画面 |
| `requirements.txt` | 依赖列表（版本锁定） |
| `scripts/setup_autodl.sh` | AutoDL 环境一键配置 |
| `scripts/run_train.sh` | tmux 后台训练启动 |
| `CLOUD_TRAINING.md` | 云 GPU 训练工作流方法论 |
